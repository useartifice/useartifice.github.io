# useartifice.github.io

The site for **Artifice** — a set of experiments in generative interfaces. A local
model composes a bespoke, interactive surface for one task, then dissolves it.

- `index.html` — landing
- `research.html` — Technical Report AR-01 (with live demos)

Static HTML/CSS/JS, no build step. Served via GitHub Pages.
